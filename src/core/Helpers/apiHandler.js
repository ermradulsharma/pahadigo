// DEPRECATED
// The logic from this file has been directly integrated into nextApiWrapper.js
// to avoid redundant body parsing and NoSQL sanitization.
export function apiHandler(handler) {
    return handler;
}
export default apiHandler;
