import module from '@/helpers/parseBody.js';

describe('Industry Standard: parseBody Module', () => {
    it('[Success] should satisfy core import requirements', () => {
        expect(module).toBeDefined();
    });
});
