import module from '@/helpers/location.js';

describe('Industry Standard: location Module', () => {
    it('[Success] should satisfy core import requirements', () => {
        expect(module).toBeDefined();
    });
});
